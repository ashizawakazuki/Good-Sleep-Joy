<!-- _item_like.html.erb にある下記のidを探してきて来て、ブロック内の（render~）HTMLに置き換える-->
<%= turbo_stream.replace "like-button-for-item-post-#{@item_post.id}" do %>
  <!-- いいねした時の状態（黄色）の見た目のパーシャルを持ってきている-->
  <%= render 'item_posts/item_unlike', item_post: @item_post %>
<% end %>