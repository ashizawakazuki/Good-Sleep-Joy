<div class="max-w-lg mx-auto p-4">
  <h1 class="text-xl font-bold my-20 text-center">ポジティブ3行日記</h1>
  <p class ="mb-6">寝る前の時間は「記憶のゴールデンタイム」と言われています。<br>
  今日起きたポジティブな出来事を３つ書き出して、それをイメージしながら眠りにつきましょう<br>
  （３つ以上書いても大丈夫です）</p>
  <%= form_with model: @diary do |f| %>

    <div class="flex flex-col mb-6"> <!-- 以下、form_withで使えるようになったもの -->
      <!-- 「:date」でモデルのカラム名を指定(ここではItemPostモデルのtitleカラム) -->
      <%= f.label :date, "日付", class: "text-lg font-medium text-gray-700 mb-2" %>  
      <!-- 上記同様 -->
      <%= f.date_field :date, class: "border border-gray-300 rounded-lg px-4 py-2 focus:ring focus:ring-blue-300 focus:outline-none" %>   
    </div>

    <div class="flex flex-col mb-6"> <!-- 以下、form_withで使えるようになったもの -->
      <!-- 「:title」でモデルのカラム名を指定(ここではItemPostモデルのtitleカラム) -->
      <%= f.label :title, "タイトル", class: "text-lg font-medium text-gray-700 mb-2" %>  
      <!-- 上記同様 -->
      <%= f.text_field :title, placeholder: "今日１日を一言で表すとどんな日？", class: "border border-gray-300 rounded-lg px-4 py-2 focus:ring focus:ring-blue-300 focus:outline-none" %>   
    </div>

    <div class="flex flex-col mb-6"> <!-- 以下、form_withで使えるようになったもの -->
      <!-- 「:content」でモデルのカラム名を指定(ここではItemPostモデルのtitleカラム) -->
      <%= f.label :content, "日記", class: "text-lg font-medium text-gray-700 mb-2" %>  
      <!-- 上記同様 -->
      <%= f.text_area :content, 
                      rows: 6, 
                      placeholder: "例\n" +
                                   "・お昼に食べたドリアが美味しかった\n" +
                                   "・家族と話す時間を多くとれた\n" +
                                   "・職場で苦手だと思っていた人に相談してみたら、意外と優しくて苦手じゃないと気がついた", 
                      class: "border border-gray-300 rounded-lg px-4 py-2 focus:ring focus:ring-blue-300 focus:outline-none" %>   
    </div>
    <!-- ボタンを中央揃え -->
    <div class="text-center my-20"> 
      <!-- 送信ボタンのデザイン -->
      <%= f.submit "投稿する", class: "bg-sky-100 rounded-lg px-6 py-2 hover:bg-sky-300 shadow-xl"%> 
    </div>
  <% end %>
</div>